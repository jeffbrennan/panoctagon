telemetry:
  enabled: false

concurrency:
  pools:
    default_limit: 1
    granularity: "op"

storage:
  sqlite:
    base_dir: data/dagster/storage

run_coordinator:
  module: dagster.core.run_coordinator
  class: QueuedRunCoordinator
