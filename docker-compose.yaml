services:
  panoctagon_backend:
    build: .
    volumes:
      - panoctagon_data:/panoctagon/data
    ports:
      - 3000:3000
volumes:
  panoctagon_data:
      driver: local
      driver_opts:
        type: 'none'
        o: 'bind'
        device: 'data'